<div class="webmentions">
<h3>Webmentions</h3>

<script id="webmention-hosted">
		(function() {
		    var sn = document.createElement("script"),
		        s = document.getElementsByTagName("script")[0],
		        url;
		    url = document.querySelectorAll
		        ? document.querySelectorAll("link[rel~=canonical]")
		        : false;
		    url = url && url[0]
		        ? url[0].href
		        : false;
		    sn.type = "text/javascript";
		    sn.async = true;
		    sn.src = "//webmention.herokuapp.com/api/embed?url=" + encodeURIComponent(url || window.location);
		    s.parentNode.insertBefore(sn, s);
		}());
</script>
<!-- End of comment presentation block -->

</div> <!-- End webmentions -->

{% if grav.twig.enable_comments_plugin %}


<!-- Displays comments -->
<div class="comments">
<h3>Comments</h3>
    {% if grav.twig.comments|length %}
<ol>        
		{% for comment in grav.twig.comments|array_reverse %}
        <li>From {{comment.author}}, {{comment.date|nicetime}}: {{comment.text}}</li>
<!-- 
		<div class="comment-content">
                    {{comment.text}}
		</div>
		<div class="commenter">
		<p>{{comment.author}}<br />{{comment.date|nicetime}}</p>
		</div>
 -->
        {% endfor %}
</ol>
    {% endif %}
</div> <!-- End comments -->
{% endif %} <!-- From if in blog-item.html.twig -->